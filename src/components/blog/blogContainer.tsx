interface BlogContainerProps {}

export const BlogContainer = ({}: BlogContainerProps) => {
  

  return (
    <main style={
      {
        padding: "16px 24px 16px 24px",
        maxWidth: "1300px",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }
    }>

    </main>    
  );
};
